rm(list=ls())
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
library(tidyverse)
library(dplyr)
airbnb_data <- airbnbsmall # store data as "airbnb_data"
#create a dummy variable for "high_rating"
airbnb_data$high_rating = ifelse(airbnb_data$n_review_scores_rating>94, 1, 0) # add
view(airbnb_data) # check again for variables
mod2 <-lm(high_rating~n_number_of_reviews+price+d_gym,data=airbnb_data) #
summary(mod2)
str(high_rating)
str(airbnb_data$high_rating)
predict(mod2, airbnb_data )
mod3 = lm(airbnb_data$high_rating~. ,data = airbnb_data)
summary(mod3)
in_predictions <- predict(mod2, airbnb_data) # calculate predictions given your model and the data
in_error <- (in_predictions-airbnb_data$high_rating)
sqrt(mean(in_error^2)) # calculate the RSME # prints 0.494
sum(high_rating$airbnb_data)
sum(high_rating$airbnb_data)
sum(airbnb_data$high_rating)
summary(airbnb_data$high_rating)
RMSE <-  function(y,y_head){
RMSE = sqrtmean((y-y_head)^2) # RMSE Formel
return(RMSE)
}
pred_mod2 = predict(mod2, airbnb_data)
rmse_mod2 = RMSE(airbnb_data$high_rating, pred_mod2) # RMSE Formel angewendet
RMSE <-  function(y,y_head){
RMSE = sqrt(mean((y-y_head)^2)) # RMSE Formel
return(RMSE)
}
pred_mod2 = predict(mod2, airbnb_data)
rmse_mod2 = RMSE(airbnb_data$high_rating, pred_mod2) # RMSE Formel angewendet
mse_mod2
rmse_mod2
rm(list=ls())
cat("\014") # command to clear console
# load packages/libraries
# install.packages("remotes") # hashtagged because already installed
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
library(tidyverse)
library(dplyr)
airbnb_data <- airbnbsmall # store data as "airbnb_data"
#create a dummy variable for "high_rating"
airbnb_data$high_rating = ifelse(airbnb_data$n_review_scores_rating>94, 1, 0) # adds the variable "high_rating" to the "airbnb_data" dataset; "ifelse" function to create a dummy variable which is =1 if n_review_scores_rating>94 and =0 otherwise
mod2 <-lm(high_rating~n_number_of_reviews+price+d_gym,data=airbnb_data)
summary(mod2)
rm(list=ls())
data(mroz, package='wooldridge')
install.packages('wooldridge')
library(wooldridge)
data(mroz, package='wooldridge')
linprob <- lm(inlf~nwifeinc+educ+exper+I(exper^2)+age+kidslt6+kidsge6,data=mroz)
summary(linprob)
mod3 = lm(airbnb_data$high_rating~. ,data = airbnb_data) # the "." on the right side indicates that all variables are taken as exogenous variables
rm(list=ls())
cat("\014") # command to clear console
# load packages/libraries
# install.packages("remotes") # hashtagged because already installed
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
library(tidyverse)
library(dplyr)
airbnb_data <- airbnbsmall # store data as "airbnb_data"
#create a dummy variable for "high_rating"
airbnb_data$high_rating = ifelse(airbnb_data$n_review_scores_rating>94, 1, 0) # adds the variable "high_rating" to the "airbnb_data" dataset; "ifelse" function to create a dummy variable which is =1 if n_review_scores_rating>94 and =0 otherwise
mod3 = lm(airbnb_data$high_rating~. ,data = airbnb_data) # the "." on the right side indicates that all variables are taken as exogenous variables
summary(mod3)
rm(list=ls())
cat("\014") # command to clear console
# load packages/libraries
# install.packages("remotes") # hashtagged because already installed
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
library(tidyverse)
library(dplyr)
airbnb_data <- airbnbsmall # store data as "airbnb_data"
#create a dummy variable for "high_rating"
airbnb_data$high_rating = ifelse(airbnb_data$n_review_scores_rating>94, 1, 0) # adds the variable "high_rating" to the "airbnb_data" dataset; "ifelse" function to create a dummy variable which is =1 if n_review_scores_rating>94 and =0 otherwise
mod2 <-lm(high_rating~n_number_of_reviews+price+d_gym,data=airbnb_data)
summary(mod2)
in_predictions <- predict(mod2, airbnb_data)
sum(in_predictions)
summary(in_predictions)
summary(airbnb_data$high_rating) # mean: 0.478
RMSE <-  function(y,y_head){ # create a function that applies the RMSE formula
RMSE = sqrt(mean((y-y_head)^2))
#return(RMSE) # specify RMSE as output of the function
}
rmse_mod2 = RMSE(airbnb_data$high_rating, in_predictions) # apply RMSE function
rmse_mod2 # prints 0.494
RMSE <-  function(y,y_head){ # create a function that applies the RMSE formula
RMSE = sqrt(mean((y-y_head)^2))
return(RMSE) # specify RMSE as output of the function
}
rmse_mod2 = RMSE(airbnb_data$high_rating, in_predictions) # apply RMSE function
rmse_mod2 # prints 0.494
mod1 <- lm(price~d_breakfast,data=airbnb_data)
rm(list=ls()) # clean environment
# install our package sozoekds
# install.packages("remotes") # hashtagged because already installed
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
# load other libraries
library(tidyverse)
library(stargazer)
library(Hmisc)
library(dplyr)
# load the data & store it as "airbnb_data"
airbnb_data <- airbnbsmall
mod1 <- lm(price~d_breakfast,data=airbnb_data)
summary(mod1)
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls())
library(wooldridge)
``
data("nbasal", package = "wooldridge")
help("nbasal", package = "wooldridge")
mod1 <-lm(wage ~ marr, data = nbasal)
summary(mod1)
rm(list=ls())
cat("\014") # command to clear console
# load packages/libraries
# install.packages("remotes") # hashtagged because already installed
library(remotes)
remotes::install_gitlab("BAQ6370/sozoekds", host="gitlab.rrz.uni-hamburg.de")
library(sozoekds)
library(tidyverse)
library(dplyr)
airbnb_data <- airbnbsmall # store data as "airbnb_data"
airbnb_data$high_rating = ifelse(airbnb_data$n_review_scores_rating>94, 1, 0) # adds the variable "high_rating" to the "airbnb_data" dataset; "ifelse" function to create a dummy variable which is =1 if n_review_scores_rating>94 and =0 otherwise
mod2 <-lm(high_rating~n_number_of_reviews+price+d_gym,data=airbnb_data)
summary(mod2)
mod3 = lm(airbnb_data$high_rating~. ,data = airbnb_data) # the "." on the right side indicates that all variables are taken as exogenous variables
summary(mod3)
in_predictions <- predict(mod2, airbnb_data) # calculate predicted values
summary(in_predictions)
in_error <- (in_predictions-airbnb_data$high_rating) # calculate the error (predicted values - observed values in the data)
sqrt(mean(in_error^2))
summary(airbnb_data$high_rating) # mean: 0.478; ranges from 0 to 1
mod1 <- lm(price~d_breakfast,data=airbnb_data) # create the object "mod1" that contains the linear regression model; "lm" function for linear regression; left to "~" stands the endogenous variable, right to "~" the exogenous variable; "airbnb_data" is the dataset used for analysis
summary(mod1)
mod2<-lm(price~d_breakfast+n_accommodates+n_number_of_reviews,data=airbnb_data) # create the object "mod2" that contains the linear regression model; "lm" function for linear regression; left to "~" stands the endogenous variable, right to "~" the exogenous variables; "airbnb_data" is the dataset used for analysis
summary(mod2)
